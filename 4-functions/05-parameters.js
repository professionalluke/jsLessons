/*
Parameters
like empty storage boxes we can name whatever
*/


function pet(animal) {
    console.log(`I have a ${animal} for a pet`);
}

pet('cat');

//challenge write a funtion that runs two parameters one for the first name and the other for the last name.

function name(firstName, lastName) {
    console.log(`Hello, my name is ${firstName} ${lastName}`);
}
name('Luke','Cureton');